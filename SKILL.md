---
name: qa-reverse-tc
description: 출시된 앱·웹 서비스를 QA 관점에서 역분석해 공통 기능 트리를 추출하고, LLM 비결정성까지 판정 가능한 테스트 케이스(TC)를 생성해 기존 시트 서식의 xlsx와 다크테마 HTML 분석 문서로 산출합니다. "역분석 TC", "TC 자동 생성", "테스트 케이스 작성", "QA 역분석", "기능 트리 추출", "TC 시트", "테스트 설계", "경쟁사 QA 분석" 등의 요청에 사용하세요. 대상이 AI 챗·LLM 기반 서비스인 경우 references/feature-tree-ai-chat.md의 기능 트리를 시드로 씁니다.
---

# QA 역분석 · TC 자동 생성

출시된 서비스를 블랙박스로 역분석해 **탐색 → 기능 트리 → TC 생성 → 산출**을 한 흐름으로 수행합니다.
한 번 만들고 끝이 아니라, 릴리즈마다 기능 트리 diff를 뜨며 **계속 수정해 나가는 자산**으로 씁니다.

## 언제 이 스킬을 쓰는가

- 경쟁 서비스나 자사 서비스를 QA 관점에서 분해해 테스트 설계를 만들 때
- 기획서가 없는 상태에서 동작하는 제품만 보고 TC를 써야 할 때
- 릴리즈마다 기능 트리 diff를 떠서 회귀 범위를 정하고 싶을 때

## 진행 원칙

**추측으로 기대값을 채우지 않는다.** 확인되지 않은 값은 `?`로 남기고 "미확인 항목" 목록에
모읍니다. 실측 전에는 그 TC의 기대 결과가 성립하지 않습니다. 공개 문서 간 수치가 상충하면
양쪽을 모두 비고에 적고 실측으로 확정합니다 — 문서를 그대로 옮겨 적으면 TC가 처음부터 틀립니다.

**용어를 지어내지 않는다.** 조직·표준에 이미 있는 말이면 그 말을 쓰고, 없으면 지어낸 것임을
밝힙니다. "표준"은 표준화 기구가 정했을 때만 씁니다(3사 공통일 뿐이면 "공통"). TN은 조직 고유
용어이므로 정의를 `depth-and-tn.md` 그대로 따릅니다 — 임의 해석 금지.

**LLM이 응답을 만드는 서비스라면 검증유형 분리가 기능 목록보다 먼저다.** 기대 결과가 단일하다는
전제가 깨지므로, 같은 기능이 검증유형에 따라 여러 TC로 갈라집니다.
→ `references/verification-types.md`를 먼저 읽으세요.

## 4단계 파이프라인

### 1단계 — 탐색

먼저 사용자에게 접근 수단을 확인합니다: 웹 접근 가능 여부, 계정 보유 여부, 성인/유료 등
게이팅된 영역까지 볼 수 있는지. 로그인이 필요한 영역은 사용자 승인 없이 진입하지 않습니다.

수집 경로를 우선순위대로 시도합니다.

1. **공식 1차 자료** — 고객센터·헬프센터, 운영정책, 업데이트 공지, 개발사 기술 블로그.
   기능 상한값(글자수·개수·용량)과 오류 문구가 여기 다 있습니다. 가장 밀도 높은 소스입니다.
2. **스토어 리스팅** — App Store / Google Play. 인앱 상품 목록·가격, 연령 등급과 사유,
   릴리즈 노트, 지원 언어, 권한. 리뷰는 알려진 결함의 1차 증거입니다.
3. **브라우저 자동화** — 웹 클라이언트가 있으면 Chrome 도구로 라우트·DOM·네트워크를 수집합니다.
   입력 필드의 `maxlength`·`pattern`·`required`와 오류 메시지를 함께 긁으면
   경계값 TC의 기대값이 자동으로 채워집니다.
4. **커뮤니티·언론** — 나무위키류 문서, 뉴스, 유저 커뮤니티. 사건·논란은 회귀 스위트의 씨앗입니다.

여러 대상을 비교한다면 대상별로 서브에이전트를 병렬 실행하고, 각 에이전트에 동일한
10개 항목(플랫폼 지원 / 계정·인증 / 탐색 / 저작 / 핵심 기능 / 고유 기능 / 수익화 /
세이프티 / 소셜 / 알려진 결함)을 지시해 결과 형식을 맞춥니다.

### 2단계 — 기능 트리 정규화

수집 결과를 Depth 계층으로 정규화합니다. → `references/depth-and-tn.md`

**명칭이 아니라 역할로 묶습니다.** 서비스마다 UI 명칭이 다릅니다(한쪽 "키워드북", 다른 쪽
"로어북"이 같은 `세계관 > 키워드 트리거 주입` 노드). 역할로 세워야 한 대상의 TC를 다른 대상에
재사용할 수 있고, 빈칸이 곧 기능 격차 분석이 됩니다.

**상태는 Depth로 쪼개지 말고 Pre-Condition으로 흡수합니다.** 로그인·인증·구독·플랫폼은 화면이
아니라 상태입니다. Depth로 쪼개면 화면마다 조합이 붙어 계층이 폭발합니다. 대신 "어떤 상태에서
어느 화면이 뜨는가"는 그 자체를 검증 대상으로 올립니다(진입 분기 케이스).

노드마다 대상별 지원 여부를 `O`(확인) / `X`(미지원 확인) / `△`(부분·조건부) / `?`(미확인)로
표기하고, 확인된 사양 수치를 비고에 남깁니다. `?`의 개수가 곧 실측 우선순위입니다.

대상이 AI 챗·LLM 서비스라면 `references/feature-tree-ai-chat.md`의 기능 트리를 시드로 쓰고
diff만 뜨세요. 처음부터 세우는 것보다 빠르고 누락이 적습니다.

### 3단계 — TC 생성

노드마다 다음 순서로 전개합니다.

1. **검증유형 판정** — 결정적 / 확률적 / 루브릭 / 금칙 중 하나. → `references/verification-types.md`
2. **케이스 전개** — 정상 · 경계 · 예외 · 우회. → `references/case-expansion.md`
3. **TN 부여** — 마지막 Depth에서 이어지는 수행 순서를 1→2→3으로 번호화. → `references/depth-and-tn.md`
4. **우선순위 산정** — High/Medium/Low. → `references/depth-and-tn.md`

기대 결과는 **판정 가능한 문장**으로 씁니다. "정상 동작한다"는 기대 결과가 아닙니다.
확률적 TC는 임계값을, 루브릭 TC는 합격선을, 금칙 TC는 시도 횟수를 기대 결과 안에 직접 적습니다.

선행 관계를 관리하고 싶으면 **TC 관계도**를 얹습니다(선택). → `references/tc-relations.md`

### 4단계 — 산출

두 갈래로 냅니다. **실행용 시트**와 **읽는 문서**.

- **TC 시트(xlsx)** — 실행·기록용. **기존 조직 서식이 있으면 반드시 그 서식을 씁니다.**
  새 레이아웃을 발명하지 마세요. → `references/tc-sheet-format.md`
- **HTML 분석 문서** — 방법론·트리·분포·관계를 읽는 다크테마 단일 파일.
  → `references/html-report-guide.md`

```bash
# 시트 (표준 서식 기본값 — 조직 서식이 다르면 그에 맞게 조정)
python scripts/build_tc_template_xlsx.py input.json -o 결과.xlsx
python /mnt/skills/public/xlsx/scripts/recalc.py 결과.xlsx   # 수식 검증 (필수)
```

입력 JSON 스키마는 각 스크립트 상단 독스트링에 있습니다. `build_tc_xlsx.py`는 트리·검증기준·
리스크 로그까지 담은 6시트 분석용 워크북을 만드는 별도 생성기입니다(조직 서식이 없을 때의 대안).

산출 후 반드시 확인합니다.
- TN이 각 케이스마다 1부터 시작해 끊김 없이 이어지는가
- Pre-Condition·Priority가 TN 1행에만 적혔는가
- Test-Step이 `~한다`, Expected-Result가 `~된다` 체인가
- (관계도를 얹었다면) 선행 TC가 실재하고 순환이 없는가
- 미확인(`?`) 항목이 "미확인 목록"에 빠짐없이 올라갔는가
- HTML은 렌더해서 눈으로 확인 — 콘솔 에러 0, 라벨 충돌·글자 깨짐 없음

## 자동화 범위의 현실

결정적 TC만 Playwright 등으로 코드 어서션까지 완전 자동화됩니다. 확률적·루브릭 TC는
실행은 자동화되지만 판정에 심판 모델 또는 사람이 필요합니다. 금칙 TC는 우회 변형을 계속
확충해야 하므로 고정된 회귀 스위트가 아니라 **지속 갱신되는 레드팀 자산**으로 다루세요.
이 비율을 산출물에 명시해야 "자동화했다"는 말이 과장되지 않습니다.

## 참고 파일

| 파일 | 내용 |
|---|---|
| `references/verification-types.md` | 검증유형 4종의 정의와 PASS/FAIL 판정 규칙 |
| `references/depth-and-tn.md` | Depth 1~7 계층, TN(테스트 넘버링), 상태→Pre-Condition, 우선순위 |
| `references/case-expansion.md` | 노드 하나를 정상·경계·예외·우회로 전개하는 규칙 |
| `references/tc-sheet-format.md` | TC 시트 컬럼 구성·문체·수식 (한 행=한 TN=한 스텝) |
| `references/tc-relations.md` | 케이스 간 선행 관계, 단계 계산, Blocked·병렬 처리 |
| `references/html-report-guide.md` | 다크테마 HTML 리포트 디자인 시스템 |
| `references/feature-tree-ai-chat.md` | AI 캐릭터 챗 서비스 공통 기능 트리 (시드) |
| `scripts/build_tc_template_xlsx.py` | TC JSON → 조직 서식 실행용 시트 |
| `scripts/build_tc_xlsx.py` | TC JSON → 6시트 분석용 워크북 (대안) |
| `scripts/norm.py` | Test-Step·Expected-Result 문체 정규화 |
