# qa-lab-miyonchat — 남은 작업 (remaining-work)

할 일의 정본입니다(규칙: `rules/remaining-work.md`). 완료 항목은 삭제되며, 완료 기록은
change-log가 담당합니다. 갱신일: 2026-08-02 (S2 홈 완료 시점).

## 다음 작업 큐

1. **SUT 구현** (`sut/`) — 진행 중. 현재 빌드 `PC웹_Ver1.0_Dev_RC5`
   - **완료**: 전역 셸(상단 바·하단 내비·푸터) · S1 로그인 · 라우팅 가드(공개 S2·S7 / 보호 S3·S4·S5·S6·S8) · 로그인 모달(막힌 동작 이어받기) · P4 간편 프로필 · T1 디버그 콘솔 · 가상 시계 · `__VN__` API · S2 홈 · 상단 바 검색·알림(**탐색/발견 전부**) · S3 페르소나 폼
   - **남은 페르소나 노드 1건** — 「설명란 프롬프트 주입 차단」(금칙). 무엇을 주입으로 판정할지가 명세에 없어 S3에 넣지 않았습니다. 입력·출력 필터와 같은 계층이므로 세이프티 슬라이스에서 판정 규칙을 정해 함께 붙이는 편이 자연스럽습니다
   - **골격 v1.4 재작업(선행)** — 카드 상세 패널 → **S3 캐릭터 페이지**(제작자·현황·출시일/업데이트·추천·하단 버튼 분기·대화방 목록·한도), 페르소나 폼 → **P5 대화 프로필 패널**(복수·Label·랜덤 완성), 방 생성 시 프로필 고정, 세이브 2층(대화방 한도 4 + 시점 슬롯 4, 로드 갈래). 청사진 §3-1의 ⚠ 줄이 사라지고 새 testid가 ✅로 올라갑니다
   - **다음 단위 — S4 대화**: 메시지 목록·자유 입력+고정 선택지 → mock 응답(시드 결정)·스트리밍 표시 → 전송 시 재화 차감(연타·실패 미차감) → 메시지 액션(편집·삭제·재생성은 최신 교환만, 과거 턴은 분기) → 호감도·관계 단계·엔딩 판정
   - 그 뒤: P1·P2·P3 패널 → S5 채팅 → S6 MY(세이프티 필터 토글 포함) → 스텁(S7·S8)
   - 마무리: **data-testid 전체 목록을 청사진 §3-1에 확정 등재**, 스모크의 STUB를 실제 SUT로 교체(test_smoke.py [주의] 참조)
   - 지켜야 할 조건: ① 코드가 명세와 어긋나면 명세를 먼저 고치고 스펙 변경으로 기록 ② 기능 트리 노드 구현 외 작업 금지(시간 도둑 1번) ③ data-testid 명명 규칙은 코드 첫 줄 전에 확정
   - 작업 요령: 정적 서버로 확인 · **CSS·JS 수정 시 `index.html`의 `?v=` 숫자 올리기** · 슬라이스 완료 시 `data.js`의 `SUT_BUILD`를 다음 RC로
2. **TC 설계·산출** (playbook STEP 6) — 구현 후 수행
   - 선행 완료(2026-08-02): 스크립트 3자 동기화 · `tc-input-master.json` · 이슈 관리 시트 편입 · **플랫폼 Web 단독 확정** · `test-case/…-tc-input-v1.0.json` 생성(목록 값·vt_note 반영, `tcs`만 비어 있음) · 케이스 전개 축 판정(청사진 §4-1)
   - 남은 것: `tcs` 배열 작성 → xlsx 생성 → Excel 오픈 검증
   - 기대값은 트리·design/에서만 가져오고, Test-Step은 구현으로 확정된 화면 용어로 씁니다
3. **자동화** — conftest(reset)·thresholds·영역별 테스트 파일 → 결정적·금칙 격리부터 → 결함 주입 매트릭스(대각선만 FAIL) → 리포트(+"SUT 한계와 검증 범위")
4. **CI·묶기** — 워크플로(경로 필터, deploy-pages 금지), 추적 매트릭스, 프로젝트 허브·용어집 생성, README 실행 GIF, 저작권 게이트 → push

## 사용자 결정 대기

- **시나리오를 유저가 고르는가** (골격 v1.4 미결) — 실측에는 캐릭터 페이지 하단에 제작자가 깔아 둔 시작 상황 라벨이 있고 트리의 「시나리오 선택 시작」도 REF입니다. 사용자 모델은 "시나리오는 대화 중 발생"이라 같은 낱말이 다른 것을 가리킵니다. 이름을 갈라 둘 다 남길지(시작 상황 / 전개), 선택을 없앨지 정해야 mock 세트의 `scenarioId` 좌표도 확정됩니다
- **프로필 개수 상한** — 트리 미확인. 실측 100이지만 SUT는 경계 검증 비용을 보고 정합니다(후보: 5)

- **자동화 리포트 템플릿(Case C)** — 기본 리포트로 시작 권고 유지, 여유 시 재논의 (3번 리포트 단계 전까지)
- **Issue No. 연결의 보존 위치** — 결과 시트는 실행 시 덮어쓰므로, 사람이 적은 Issue No.가 지워집니다. TC 설계 단계에서 연결을 어디에 남길지 결정 필요(제안: 이슈 관리 시트가 정본이고 결과 시트는 표시만)
- **트리 노드의 조사 후보 표기** — 노드 설명에 「값은 design, 후보 12/1,000」처럼 조사 단계의 후보 수치가 남아 있습니다. 확정값과 다른 숫자라 헷갈릴 수 있어(확정 12/12/1,000) 지우는 안과, 출처 감각이 노드에서 읽히므로 남기는 안이 있습니다. 지우면 출처는 `rationale/…-addition-rationale.md`에서만 읽힙니다
- **`build_tc_xlsx.py` 존치** — 분석 문서를 HTML로 내기로 해 역할 중복, 보류 중
- **미push 커밋 push 시점** — 2026-08-02 `3567cdd`까지 push 완료. 이후 누적분(골격 v1.2·SUT 첫 슬라이스·이슈 체계·디버그 콘솔·S2 홈)은 대기 중이며 저작권 게이트에 걸릴 항목 없음

## 백로그

- CLAUDE.md에 `automation/` 하위 구조(`tests`·`report`·`result/history`)와 "정본과 파일 지위" 표 반영 (현재 정본은 change-log §확정된 결정)
- `scripts/gen_test_skeleton.py` — 시간 부족 시 버리는 순서 2번, 수동 작성으로 대체 가능
- 플레이툰 잔여 실측 소소 2건 — 내 작품 탭·빈 검색 결과 UX (`analysis/…-plaitoon-inventory.md` B-3. 홈 칩별 목록 구성은 2026-08-02 실측 완료)
- MY 프로필 편집 — 2026-08-02 미편입 확정, 추후 편입 가능성 있음(사용자 의향). 편입 시 트리 개정 필요
- **리그레션 TC 세트** — 결함에서 출발하는 케이스는 기능 TC와 섞지 않고 별도 세트로 만들어 진행합니다(2026-08-02 사용자 확정). 결함이 쌓인 뒤 착수하며, 기능 TC(v1.0)와 파일·번호 체계를 어떻게 가를지는 착수 시점에 결정
- 워크스페이스 잔재 파일 정리 — SKILL.md·feature-tree-ai-chat.md 제거(사용자 요청 대기)
