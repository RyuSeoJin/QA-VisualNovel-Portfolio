# qa-lab-miyonchat — 세이브 스키마 (save-schema)

## 0. 문서 지위

- ②SUT 레이어 사양입니다. 슬롯 스냅샷에 **담기는 필드의 정본**이며, `getSave(slot)`의 반환
  구조이자 자동화가 대조하는 기준입니다
- 슬롯 수·과금·덮어쓰기 등 기획 규칙은 `design/…-system-spec.md` §6이 정본입니다

## 1. 저장 계층

- 모든 런타임 상태는 **메모리에만** 있습니다. 브라우저 새로고침(F5) 시 초기화되며, 상태
  재현은 테스트 설비(T1·`__VN__`)가 담당합니다 — 갱신 버튼이 F5를 대체하는 이유입니다
- 슬롯은 방 스코프 상태의 **깊은 복사 스냅샷**입니다. 저장 후 원본 방이 변해도 스냅샷은
  변하지 않아야 하며(얕은 복사 참조 공유가 save-leak 결함), 로드는 방 스코프 전체를
  스냅샷으로 치환합니다
- 계정 스코프(재화·대화 프로필 목록·좋아요 등)는 스냅샷에 **담지 않습니다** — 로드해도 재화가
  복구되지 않는 근거입니다. 다만 그 방이 쓰는 **프로필 값의 사본**은 방 스코프에 있으므로
  스냅샷에 함께 담깁니다(프로필을 지워도 그 방의 대화가 그대로 남아야 하기 때문입니다)
- 저장 단위는 **대화방 안의 시점**입니다. 대화방 자체는 세이브가 아니라 캐릭터 페이지가
  관리하는 목록이며, 한도와 삭제 규칙은 system-spec §6이 정본입니다

## 2. 슬롯 스냅샷 구조

```json
{
  "slot": 1,
  "savedAtDay": "2026-08-02",
  "summary": "호기심 · 12턴 · 학교 앞",
  "room": {
    "messages": [
      {
        "id": "m1",
        "speaker": "ai | user",
        "kind": "message | direction",
        "text": "…",
        "deltaAffection": 0,
        "memoryAdd": null
      }
    ],
    "affection": 34,
    "temperature": "따뜻함",
    "nickname": "선배",
    "profile": { "id": "p1", "name": "서진", "nickname": "너", "label": "연애 모드" },
    "memories": [
      { "id": "mem1", "text": "민트초코를 싫어함", "pinned": false }
    ],
    "seedPath": { "seed": 42, "turn": 12 }
  }
}
```

| 필드 | 규칙 |
|---|---|
| `slot` | 1~4. 방마다 독립 |
| `savedAtDay` | 저장 시점의 기준일(가상 시계 — 실시각 없음) |
| `summary` | 슬롯 정보 표시용 — 단계명·턴수·현재 장소 |
| `room.messages[].kind` | `message`(대화수 집계 대상) / `direction`(연출 — 집계 제외) |
| `room.messages[].deltaAffection` | 그 턴이 만든 호감도 변화 — 되돌림(삭제·재생성)의 취소 단위 |
| `room.affection` | 호감도 원값. 관계 단계는 저장하지 않고 임계로 파생합니다(이중 저장 시 어긋남 방지) |
| `room.profile` | 그 방에 고정된 대화 프로필의 사본 — 계정의 프로필 목록이 바뀌어도 방은 저장된 값으로 답합니다 |
| `room.seedPath` | mock 경로 위치 — 로드 후 같은 시드 흐름이 이어지는 근거 |

## 3. 격리 규칙 (검증 경계)

- **슬롯 간**: 다른 슬롯의 값이 혼입되면 실패 — `getSave(1)`과 `getSave(2)`는 서로 독립
- **방·분기 간**: 분기 방의 세이브는 원본 방 슬롯과 무관합니다. 분기는 로드의 「새 방으로」
  갈래로 생기며, 만들어진 뒤에는 원본을 참조하지 않습니다
- **프로필 간**: 다른 프로필로 시작한 방의 이름·호칭이 섞이면 실패입니다
- **계정 간**: 계정 전환 시 상대 계정의 슬롯이 보이거나 로드되면 실패
- 이 세 경계는 화면만으로 검증되지 않으므로 `getSave()`·`getState()` 대조가 유일한
  검증 수단입니다
